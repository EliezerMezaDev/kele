---
import JobCard from "../JobCard.astro";
import { JOB_SOURCE } from "../../lib/data.ts";

const jobs: Kele.Job[] = JOB_SOURCE;
---

<section class="grid grid-cols-3">
  {jobs.map((j) => <JobCard {...j} />)}
</section>
